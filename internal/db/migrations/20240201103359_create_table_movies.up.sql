CREATE TABLE Movies (
    ID int NOT NULL,
    Title varchar(255) NOT NULL,
    Year varchar(255),
    ImdbId varchar(255),
    Type varchar(255),
    Poster varchar(255),  
    PRIMARY KEY (ID)
);

Commit;
